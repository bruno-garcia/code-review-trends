{
  "id": "37e3042f",
  "title": "Task 4: Update pipeline consumers of bots.ts (sync.ts, bigquery.ts, tools)",
  "tags": [
    "product-expansion"
  ],
  "status": "closed",
  "created_at": "2026-02-15T05:15:25.400Z"
}

Plan: .pi/plans/2026-02-14-product-model-bot-expansion.md

## Task
Update all pipeline files that import from bots.ts to use the new single-login API. The change is: `b.github_logins` (array) → `b.github_login` (string).

## Files
- `pipeline/src/sync.ts` (modify) — BOT_BY_LOGIN, BOT_LOGINS usage unchanged (maps built the same way)
- `pipeline/src/cli.ts` (modify) — BOT_LOGINS usage, import PRODUCTS, update sync-bots command
- `pipeline/src/tools/discover-bots.ts` (modify) — BOT_LOGINS usage (should be fine)
- `pipeline/src/tools/inspect-data.ts` (modify) — may reference bot fields
- `pipeline/src/tools/status.ts` (modify) — show product count
- `pipeline/src/tools/validate-schema.ts` (modify) — update expected schema

## Details
Most of these files use BOT_BY_LOGIN, BOT_LOGINS, BOT_BY_ID maps which are already rebuilt correctly in Task 2. The main changes are:
- `cli.ts`: import PRODUCTS, call syncProducts in sync-bots
- `validate-schema.ts`: add products table to expected schema
- `inspect-data.ts`: optionally show product info
- `status.ts`: show product count alongside bot count

Also check if any file directly accesses `.github_logins` (plural) on a BotDefinition and update to `.github_login`.

## Acceptance Criteria
- [ ] No TypeScript errors across the pipeline workspace
- [ ] `npm run build:pipeline` succeeds
- [ ] All imports resolve correctly

## Depends On
- Task 2 (bots.ts)
- Task 3 (clickhouse.ts)

## Completed

- Verified no pipeline files reference the old `github_logins` plural field
- Added `products` table to validate-schema.ts expected schema (id, name, website, description, brand_color, avatar_url)
- Added `product_id`, `brand_color`, `avatar_url` columns to expected bots table schema
- `npx tsc --noEmit` passes clean across the pipeline workspace
