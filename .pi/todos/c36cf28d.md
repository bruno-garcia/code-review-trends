{
  "id": "c36cf28d",
  "title": "Task 9: Update app pages — product-level display, brand colors",
  "tags": [
    "product-expansion"
  ],
  "status": "closed",
  "created_at": "2026-02-15T05:16:24.474Z"
}

Plan: .pi/plans/2026-02-14-product-model-bot-expansion.md

## Task
Update the app's pages to show product-level data instead of raw bot-level. Use brand colors and avatars. Keep the `/bots/` URL but display products.

## Files
- `app/src/app/page.tsx` (modify) — leaderboard uses product summaries
- `app/src/app/bots/page.tsx` (modify) — listing shows products
- `app/src/app/bots/[id]/page.tsx` (modify) — detail page shows product with per-bot breakdown
- `app/src/components/charts.tsx` (modify) — use brand_color for chart lines if available

## Details

### Home page (page.tsx):
- Leaderboard table: show product name, avatar, brand color indicator
- Charts: use product-level weekly activity (getWeeklyActivityByProduct)
- AI share chart: unchanged (human vs all bots aggregate)

### Bots listing (bots/page.tsx):
- Show products, not individual bots
- Each card: product avatar, name, brand color accent, total reviews (aggregated), growth
- Link to `/bots/[product_id]`

### Product detail (bots/[id]/page.tsx):
- Header: product name, avatar, brand color, website link
- Aggregate stats: total reviews, repos, growth (sum of all bots)
- Weekly activity chart: product-level line
- If multi-bot product: show breakdown section with per-bot stats and timeline showing transitions
- Reactions chart: sum across bots

### Charts:
- Use `brand_color` from product for chart line colors
- Fall back to existing color palette if brand_color is missing

## Acceptance Criteria
- [ ] Home page leaderboard shows products
- [ ] Bots listing page shows products with avatars and colors
- [ ] Product detail page shows aggregate stats
- [ ] Multi-bot products show per-bot breakdown
- [ ] Brand colors used in charts
- [ ] No TypeScript/build errors
- [ ] Pages render with seed data

## Depends On
- Task 8 (app queries)
