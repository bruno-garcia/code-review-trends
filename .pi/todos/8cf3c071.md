{
  "id": "8cf3c071",
  "title": "Task 2: Rewrite bots.ts — ProductDefinition, expanded BOTS, single github_login",
  "tags": [
    "product-expansion"
  ],
  "status": "closed",
  "created_at": "2026-02-15T05:15:05.205Z"
}

Plan: .pi/plans/2026-02-14-product-model-bot-expansion.md

## Task
Rewrite `pipeline/src/bots.ts` to introduce `ProductDefinition` type and `PRODUCTS` array. Expand `BOTS` to 28 entries across 23 products. Change `github_logins: string[]` back to `github_login: string` (one login per bot). Add `product_id`, `brand_color`, `avatar_url` to `BotDefinition`.

## Files
- `pipeline/src/bots.ts` (rewrite)

## Details

### New types:
```ts
export type ProductDefinition = {
  id: string;
  name: string;
  website: string;
  description: string;
  brand_color: string;
  avatar_url: string;
};

export type BotDefinition = {
  id: string;
  product_id: string;
  name: string;
  github_login: string;  // singular now
  website: string;
  description: string;
  brand_color: string;
  avatar_url: string;
};
```

### Products (23):
CodeRabbit, GitHub Copilot, CodeScene, Sourcery, Ellipsis, Qodo, Greptile, Sentry, Baz, Graphite, CodeAnt, Windsurf, Cubic, Cursor Bugbot, Gemini Code Assist, Bito, Korbit, Claude, OpenAI Codex, Jazzberry, Mesa, LinearB, Augment Code

### Multi-bot products:
- Qodo: codium-pr-agent[bot], qodo-merge[bot], qodo-merge-pro[bot]
- Sentry: sentry[bot], seer-by-sentry[bot], codecov-ai[bot]
- LinearB: gitstream-cm[bot], linearb[bot]

### Derived maps to update:
- `BOT_BY_LOGIN` — Map<string, BotDefinition> using `b.github_login` (singular)
- `BOT_BY_ID` — same
- `BOT_LOGINS` — Set from `b.github_login`
- Add: `PRODUCT_BY_ID` — Map<string, ProductDefinition>
- Add: `BOTS_BY_PRODUCT` — Map<string, BotDefinition[]>

### Brand colors / avatar_urls:
Use colors from the external devtools.json list. For bots not in that list (CodeScene), pick a reasonable color. Avatar URLs from the external list or GitHub API.

## Acceptance Criteria
- [ ] 23 products defined in PRODUCTS array
- [ ] 28 bots defined in BOTS array, each with one github_login
- [ ] Every bot has a valid product_id referencing a PRODUCTS entry
- [ ] No duplicate logins, no duplicate bot ids, no duplicate product ids
- [ ] Derived maps (BOT_BY_LOGIN, BOT_LOGINS, PRODUCT_BY_ID, BOTS_BY_PRODUCT) built correctly
- [ ] Qodo name is just "Qodo" (not "Qodo (formerly CodiumAI)")

## Depends On
- None (can be done independently)
