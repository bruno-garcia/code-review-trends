{
  "id": "429a1924",
  "title": "Task 3: Update pipeline clickhouse.ts — syncProducts, updated syncBots",
  "tags": [
    "product-expansion"
  ],
  "status": "closed",
  "created_at": "2026-02-15T05:15:14.453Z"
}

Plan: .pi/plans/2026-02-14-product-model-bot-expansion.md

## Task
Update pipeline ClickHouse writer to sync products table and write the new bot fields (product_id, brand_color, avatar_url). Simplify syncBots since each bot now has a single github_login.

## Files
- `pipeline/src/clickhouse.ts` (modify)

## Details

### Add `syncProducts()`:
```ts
export async function syncProducts(client: ClickHouseClient, products: ProductDefinition[]): Promise<void> {
  // Ensure products table exists (migration)
  // Insert product rows
}
```

### Update `syncBots()`:
- Write `product_id`, `brand_color`, `avatar_url` to `bots` table
- `bot_logins` insert uses `b.github_login` (singular, not flatMap over array)
- Still ensure `bot_logins` table exists for migration

### Update CLI (`cli.ts`):
- `sync-bots` command also calls `syncProducts()` before `syncBots()`
- Import PRODUCTS from bots.ts

## Acceptance Criteria
- [ ] `syncProducts()` writes all products to ClickHouse
- [ ] `syncBots()` writes product_id, brand_color, avatar_url
- [ ] `syncBots()` writes one login per bot to bot_logins
- [ ] `sync-bots` CLI command syncs both products and bots
- [ ] Types updated (no TypeScript errors)

## Depends On
- Task 1 (schema)
- Task 2 (bots.ts types)

## Completed

- Added `syncProducts()` to `pipeline/src/clickhouse.ts` with ensure-table-exists migration and product row insert
- Extended `syncBots()` to write `product_id`, `brand_color`, `avatar_url` fields
- Updated `cli.ts` to import `PRODUCTS` and `syncProducts`, call products sync before bots sync
- TypeScript compiles clean, sync-bots command tested successfully (23 products, 28 bots)
