{
  "id": "5543beef",
  "title": "Task 11: Verify full build + lint + typecheck passes",
  "tags": [
    "product-expansion"
  ],
  "status": "closed",
  "created_at": "2026-02-15T05:16:40.723Z"
}

Plan: .pi/plans/2026-02-14-product-model-bot-expansion.md

## Task
Final verification pass. Run the full CI suite locally: lint, typecheck, build, pipeline tests, e2e tests. Fix any remaining issues.

## Files
- Any files with remaining issues

## Details

```bash
npm run lint
npm run build
npm run build:pipeline
npm test --workspace=pipeline
npm run test:e2e
```

Also verify:
- `docker compose down -v && docker compose up -d` — seed data loads cleanly
- `npm run inspect` — shows all 23 products / 28 bots
- `npm run validate` — schema matches expectations

## Acceptance Criteria
- [ ] `npm run lint` passes (all workspaces)
- [ ] `npm run build` passes (app)
- [ ] `npm run build:pipeline` passes
- [ ] `npm test --workspace=pipeline` passes
- [ ] `npm run test:e2e` passes
- [ ] Seed data loads in fresh ClickHouse
- [ ] `npm run inspect` shows expanded bot/product set

## Depends On
- All previous tasks

## Completed

All CI checks pass with no code changes needed:

- ✅ `npm run lint` — 0 errors (3 warnings for `<img>` usage, expected)
- ✅ `npm run build` — app builds successfully
- ✅ `npm run build:pipeline` — pipeline builds successfully
- ✅ `npm test --workspace=pipeline` — 17/17 tests pass
- ✅ `npm run validate` — schema validation passed (7 tables)
- ✅ Fresh `docker compose down -v && up -d` — seed data loads, 28 bots visible
- ✅ `npm run inspect` — shows 28 bots across products
- ✅ Playwright e2e — 24/24 tests pass
