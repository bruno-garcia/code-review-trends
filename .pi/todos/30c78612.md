{
  "id": "30c78612",
  "title": "Task 6: Update sync.test.ts for single-login bot model",
  "tags": [
    "product-expansion"
  ],
  "status": "closed",
  "created_at": "2026-02-15T05:15:43.623Z"
}

Plan: .pi/plans/2026-02-14-product-model-bot-expansion.md

## Task
Update the sync integration tests to work with the new single-login bot model. The tests use BOT_BY_LOGIN and BOT_LOGINS which are rebuilt in Task 2 — mostly the tests should just work, but verify and fix any breakage.

## Files
- `pipeline/src/sync.test.ts` (modify)

## Details
The sync tests use a fake DataFetcher and the real bot registry. With 28 bots now, the tests need to:
- Still work with the first bot login from BOT_LOGINS
- Handle the expanded bot set in backfill state tracking
- Verify pipeline_state bot_logins key changes trigger re-fetch (already tested, but with more logins now)

Mostly mechanical — run tests, fix what breaks.

## Acceptance Criteria
- [ ] `npm test --workspace=pipeline` — sync tests pass
- [ ] Backfill resume test still validates bot set change detection

## Depends On
- Task 2 (bots.ts)
- Task 3 (clickhouse.ts)

Completed: All 12 sync tests pass without changes. The tests use BOT_LOGINS and BOT_BY_LOGIN which work identically after the single-login refactor. No code modifications needed.
